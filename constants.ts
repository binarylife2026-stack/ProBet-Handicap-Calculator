
export const ASIAN_HANDICAP_LINES = Array.from({ length: 33 }, (_, i) => (i - 16) * 0.25);
export const EUROPEAN_HANDICAP_LINES = Array.from({ length: 17 }, (_, i) => i - 8);

export const DEFAULT_STAKE = 100;
export const DEFAULT_ODDS = 1.95;
